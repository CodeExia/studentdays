#include <stdio.h>

int main()
{

    for (int t = 1, int temp,int j; t < sizeofarray; t++)
    {
        temp = array[t] 
        while (j >= 0 && array[t] > temp)
        {
            array[j+1] = array[j];
            j--;
        }
    }
    a[j+1] = temp;
}