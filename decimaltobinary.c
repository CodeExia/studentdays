#include <stdio.h>

int bruh(char keys[]);
int main()
{
    int input;

    scanf("%d", input);

    printf("%d", bruh(input));

    return 0;
}

int binaryDecimal(char keys[])
{
    int binary;

    binary = keys;

    return binary;
}